{
    "name": "fyp-hostel-management-system",
    "version": "1.0.0",
    "description": "Hostel Management System Root",
    "scripts": {
        "start": "node server/src/server.js",
        "dev": "npm run dev --prefix server",
        "install-all": "npm install && npm install --prefix server",
        "build": "npm install && cd server && npm install",
        "vercel-build": "npm install && cd server && npm install"
    },
    "dependencies": {
        "bcryptjs": "^3.0.3",
        "cors": "^2.8.5",
        "dotenv": "^17.2.3",
        "express": "^5.2.1",
        "jsonwebtoken": "^9.0.3",
        "mongodb": "^6.8.0",
        "mongoose": "^9.1.3",
        "morgan": "^1.10.1"
    }
}
